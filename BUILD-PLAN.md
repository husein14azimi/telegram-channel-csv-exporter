the project (already in `tg_channel_export.py`) works properly. i want to add a feature to it. i want frontend as well. the project GUI should be in web format, as we can host the static web app on a free hosting provider (like github pages) that sends requests to the desired endpoints. also, users can see a mock data of their desired channel history. this way, besides csv format, they can get html export format which enables them to see the chat history. each data type that the python `tg_channel_export.py` project can export should be used in the html output. so the features are 1. static web app frontend as the project's GUI in web interface (requires re-writing the python logic and syntax in javascript to work in web) and 2. html output to show the messages in real graphical format, not csv/data.